import Pricing from "@/components/global/Pricing";

export const metadata = {
  title: 'Pricing | Shipyards Web Studio',
  description: 'Learn more about our flexible pricing packages made for every type of business owner.',
};

export default function AboutPage() {
  return (<Pricing />);
}