export type NavItem = {
  href: string;
  label: string;
};

export type PortfolioItem = {
  name: string;
  description: string;
  image: string;
  link: string;
  caseStudyLink: string;
};

export type TestimonialItem = {
  text: string;
  author: string;
}